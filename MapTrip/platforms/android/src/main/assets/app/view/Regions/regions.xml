<Page loaded="onPageLoaded">
   <StackLayout class="registerformImage" orientation="vertical" verticalAlignment="center" TextWrapt="true" width="strech" height="strech" >
<StackLayout class="registerform" width="strech" height="strech">
  <ListView items="{{ localImagesArray }}">
  	<ListView.itemTemplate>
  		<Label text="{{ Name }}" >
  		<Image src="{{ Picture }}" stretch="none"  />

  		</Label>
  </ListView.itemTemplate>
</ListView>
  </StackLayout>
  </StackLayout> 
</Page>